[workspace]
members = ["tool_manager", "gui_app", "shared", "xbogus", "live_recorder"]
resolver = "2"
default-members = ["gui_app"]

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["emojiiii"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/emojiiii/magekit-app"

[workspace.dependencies]
# Core dependencies
anyhow = "1.0"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
uuid = { version = "1.0", features = ["v4", "serde"] }
reqwest = { version = "0.11", features = ["json"] }
url = "2.4"

# GPUI dependencies
gpui = { version = "*" }
gpui-component = { version = "*" }
gpui-component-assets = { version = "*" }
reqwest_client = { git = "https://github.com/longbridge/gpui-component.git", tag = "v0.4.2" }

# Configuration
toml = "0.8"
dirs = "5.0"

# Logging
tracing = "0.1"
tracing-subscriber = "0.3"

# Time handling
chrono = { version = "0.4", features = ["serde"] }

[profile.dev]
codegen-units = 16
debug = "limited"
split-debuginfo = "unpacked"

[profile.dev.package]
resvg = { opt-level = 3 }
rustybuzz = { opt-level = 3 }
taffy = { opt-level = 3 }
ttf-parser = { opt-level = 3 }
